import LoginInputInfoPage from "./login-form/LoginInputInfoPage";

const LoginPage = () => {
  return (
    <div className="w-full h-full bg-main bg-opacity-40">
      <div className="flex items-center justify-center py-8">
        <LoginInputInfoPage />
      </div>
    </div>
  );
};

export default LoginPage;
